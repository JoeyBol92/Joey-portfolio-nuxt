import type { AppHeadMetaObject } from "nuxt/schema";

export default {
  htmlAttrs: {
    lang: "nl",
  },

  link: [{ rel: "icon", type: "image/png", href: "/favicon.png" }],
} as AppHeadMetaObject;
